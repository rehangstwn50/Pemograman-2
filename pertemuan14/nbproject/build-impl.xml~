<?xml version="1.0" encoding="UTF-8"?>
<!--
*** GENERATED FROM project.xml - DO NOT EDIT  ***
***         EDIT ../build.xml INSTEAD         ***
-->
<project name="pertemuan14-impl" default="default" basedir="..">
    <fail message="Please build using Ant 1.7.1 or higher.">
        <condition>
            <not>
                <antversion atleast="1.7.1"/>
            </not>
        </condition>
    </fail>
    <property file="nbproject/private/private.properties"/>
    <property file="${user.properties.file}"/>
    <property file="nbproject/project.properties"/>

    <condition property="have.tests">
        <or>
            <available file="${test.src.dir}" type="dir"/>
        </or>
    </condition>

    <macrodef name="debug-classpath-property">
        <attribute name="name"/>
        <attribute name="classpath"/>
        <sequential>
            <pathconvert property="@{name}" refid="@{classpath}"/>
        </sequential>
    </macrodef>

    <target name="-pre-init">
    </target>

    <target name="-init-private" depends="-pre-init">
        <property file="nbproject/private/private.properties"/>
    </target>

    <target name="-init-user" depends="-pre-init,-init-private">
        <property file="${user.properties.file}"/>
    </target>

    <target name="-init-project" depends="-pre-init,-init-private,-init-user">
        <property file="nbproject/project.properties"/>
    </target>

    <target name="-do-init" depends="-pre-init,-init-private,-init-user,-init-project,-init-macrodef-property">
        <available file="${src.dir}" property="have.sources"/>
    </target>

    <target name="-init-check" depends="-do-init">
        <fail unless="build.dir">Must set build.dir</fail>
        <fail unless="dist.dir">Must set dist.dir</fail>
        <fail unless="build.web.dir">Must set build.web.dir</fail>
        <fail unless="dist.war">Must set dist.war</fail>
        <fail unless="web.docbase.dir">Must set web.docbase.dir</fail>
    </target>

    <macrodef name="property" uri="http://www.netbeans.org/ns/j2se-project/1">
        <attribute name="name"/>
        <attribute name="value"/>
        <sequential>
            <property name="@{name}" value="@{value}"/>
        </sequential>
    </macrodef>

    <target name="-init-macrodef-property" depends="-pre-init,-init-private,-init-user,-init-project">
    </target>

    <target name="-init-macrodef-javac">
        <macrodef name="javac" uri="http://www.netbeans.org/ns/j2se-project/3">
            <attribute name="srcdir" default="${src.dir}"/>
            <attribute name="destdir" default="${build.classes.dir}"/>
            <attribute name="classpath" default="${javac.classpath}"/>
            <attribute name="debug" default="${javac.debug}"/>
            <attribute name="listfiles" default="false"/>
            <element name="customize" optional="true"/>
            <sequential>
                <javac srcdir="@{srcdir}" destdir="@{destdir}" debug="@{debug}"
                       deprecation="${javac.deprecation}" encoding="${source.encoding}"
                       listfiles="@{listfiles}" source="${javac.source}" target="${javac.target}">
                    <classpath>
                        <path path="@{classpath}"/>
                    </classpath>
                    <customize/>
                </javac>
            </sequential>
        </macrodef>
    </target>

    <target name="init" depends="-pre-init,-init-private,-init-user,-init-project,-do-init,-init-check,-init-macrodef-property,-init-macrodef-javac">
    </target>

    <target name="deps-jar" depends="init">
    </target>

    <target name="-pre-pre-compile" depends="init,deps-jar">
        <mkdir dir="${build.web.dir}"/>
        <copy todir="${build.web.dir}">
            <fileset dir="${web.docbase.dir}"/>
        </copy>
        <mkdir dir="${build.classes.dir}"/>
    </target>

    <target name="-pre-compile" depends="-pre-pre-compile">
    </target>

    <target name="-do-compile" depends="-pre-pre-compile,-pre-compile" if="have.sources">
        <j2seproject3:javac srcdir="${src.dir}" destdir="${build.classes.dir}" classpath="${javac.classpath}" xmlns:j2seproject3="http://www.netbeans.org/ns/j2se-project/3"/>
    </target>

    <target name="-post-compile" depends="-do-compile">
    </target>

    <target name="compile" depends="-pre-pre-compile,-pre-compile,-do-compile,-post-compile" description="Compile project.">
    </target>

    <target name="-pre-dist" depends="compile">
        <mkdir dir="${dist.dir}"/>
    </target>

    <target name="dist" depends="-pre-dist" description="Build distribution (WAR).">
        <war destfile="${dist.war}" webxml="${webinf.dir}/web.xml" compress="${jar.compress}">
            <fileset dir="${build.web.dir}"/>
        </war>
    </target>

    <target name="clean" description="Clean build products.">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="default" depends="dist"/>
</project>
